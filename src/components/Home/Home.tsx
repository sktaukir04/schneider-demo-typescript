import React from 'react';
import Login from '../Login/Login';
import Footer from '../Footer/Footer';

const Home: React.FC = () => {
  return (
    <div>
      <Login />
      <Footer />
    </div>
  );
};

export default Home;
