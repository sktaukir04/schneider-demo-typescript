import React from 'react';
import Hero from '../Login/Login';
import Footer from '../Footer/Footer';

const Home: React.FC = () => {
  return (
    <div>
      <Hero />
      <Footer />
    </div>
  );
};

export default Home;
